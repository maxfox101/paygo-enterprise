#include "domain.h"

/*
 * РЕАЛИЗАЦИЯ ДОМЕНА ТРАНСПОРТНОГО КАТАЛОГА
 * 
 * В этом файле размещены классы/структуры, которые являются частью предметной области
 * (domain) транспортного каталога и НЕ ЗАВИСЯТ от конкретной реализации справочника.
 * 
 * ПРИНЦИПЫ РАЗДЕЛЕНИЯ:
 * - Структуры Stop и Bus определены в заголовочном файле domain.h
 * - Они представляют "чистую" бизнес-логику без привязки к хранению данных
 * - Это позволяет использовать их в разных компонентах системы:
 *   * transport_catalogue - для хранения и поиска
 *   * map_renderer - для визуализации карты
 *   * json_reader - для обработки запросов
 * 
 * АРХИТЕКТУРНЫЕ ПРЕИМУЩЕСТВА:
 * 1. Независимость компонентов - map_renderer не зависит от transport_catalogue
 * 2. Переиспользование - одни и те же структуры в разных модулях
 * 3. Тестируемость - можно тестировать домен отдельно от инфраструктуры
 * 4. Расширяемость - легко добавлять новые типы транспорта
 * 
 * ТЕКУЩЕЕ СОСТОЯНИЕ:
 * - Все структуры определены в заголовочном файле (header-only)
 * - Реализация методов встроена в определения классов
 * - При необходимости здесь можно добавить сложную логику домена
 */ 